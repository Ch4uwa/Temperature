#include "pch.h"
#include "Program.h"





int main()
{
	Program p;
	p.run();
	
	//TODO Search avg value.

	// Binary search
	/*int search{ 4678 };

	int max = vec.size();
	int min{};
	int pos = max / 2;
	do
	{
		if (search < vec[pos])
		{
			max = pos;
			pos = ((max + min) / 2);
		}
		else if (search > vec[pos])
		{
			min = pos;
			pos = ((max + min) / 2);
		}
	} while (search != vec[pos]);
	LOG("Found" << vec[pos] << " at " << pos);
*/

	std::cin.get();
}

